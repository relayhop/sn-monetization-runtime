name: sn_radar
'on':
  - push
  - pull_request
concurrency:
  group: sn-radar
  cancel-in-progress: false
permissions:
  contents: write
  issues: write
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
